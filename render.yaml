# Render blueprint: Go API on the free tier, stateless — the snapshot lives in
# Firestore (STORAGE_BACKEND=firestore), so spin-downs/redeploys lose nothing.
# Frontend is served separately by Firebase Hosting.
services:
  - type: web
    name: money-tracker-api
    runtime: docker
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /health
    envVars:
      - key: STORAGE_BACKEND
        value: firestore
      - key: FIRESTORE_PROJECT_ID
        value: expense-tracker-2577a
      # Paste the service-account key JSON in the Render dashboard (never commit it).
      - key: GOOGLE_APPLICATION_CREDENTIALS_JSON
        sync: false
      - key: FEATURES
        value: all
      - key: ALLOWED_ORIGINS
        value: https://expense.theharshsharma.in,https://expense-tracker-2577a.web.app,https://expense-tracker-2577a.firebaseapp.com
      - key: FRONTEND_URL
        value: https://expense.theharshsharma.in
      # Hosting and the API are on different domains, so session cookies need
      # SameSite=None; Secure.
      - key: CROSS_SITE_COOKIES
        value: "on"
      - key: SESSION_SECRET
        generateValue: true
      # Login — set at least one of these in the dashboard. While both are
      # unset the API runs OPEN (no login): anyone with the URL can
      # read/write data.
      #
      # Simple mode: shared app password + internal-integration token for sync.
      # Set the bcrypt hash (go run ./cmd/hashpw '<password>'), not the password.
      - key: APP_PASSWORD_HASH
        sync: false
      - key: NOTION_TOKEN
        sync: false
      # OAuth mode ("Sign in with Notion"; needs a public Notion integration).
      - key: NOTION_CLIENT_ID
        sync: false
      - key: NOTION_CLIENT_SECRET
        sync: false
      # Set to https://<this-service>.onrender.com/api/auth/notion/callback
      - key: NOTION_REDIRECT_URI
        sync: false
      - key: ALLOWED_NOTION_EMAILS
        sync: false
      - key: QUOTES_REFRESH
        value: "on"
